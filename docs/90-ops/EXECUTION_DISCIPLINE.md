# Execution Discipline (Project Rule)

- Use superpowers workflow: brainstorm -> plan -> subagent execution -> review -> finish.
- Summarize and archive key decisions continuously.
- Delegate parallelizable tasks to minimax by default; main agent performs final consistency checks.
- After each design/plan update:
  1) commit to data repo
  2) push to GitHub
  3) sync summary to Notion dashboard page
